---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: logicalrouterports.multiovn.io
spec:
  group: multiovn.io
  names:
    kind: LogicalRouterPort
    listKind: LogicalRouterPortList
    plural: logicalrouterports
    singular: logicalrouterport
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.uuid
      name: UUID
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              enabled:
                type: boolean
              externalIDs:
                additionalProperties:
                  type: string
                type: object
              haChassisGroup:
                type: string
              ipv6Prefix:
                items:
                  type: string
                type: array
              ipv6RaConfigs:
                additionalProperties:
                  type: string
                type: object
              logicalRouterName:
                type: string
              logicalRouterNamespace:
                type: string
              mac:
                type: string
              networks:
                items:
                  type: string
                type: array
              options:
                additionalProperties:
                  type: string
                type: object
              peer:
                type: string
            type: object
          status:
            properties:
              logicalRouterUUID:
                type: string
              uuid:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
